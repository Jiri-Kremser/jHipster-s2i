#!/bin/bash -e
cat <<EOF
This is the jhipster-centos7 S2I image:
To use it, install S2I: https://github.com/openshift/source-to-image

Sample invocation:

s2i build https://github.com/jiri-kremser/equoid-ui-prototype jhipster-centos7 sample-app

You can then run the resulting image via:
docker run --rm -it -p 8080:8080 sample-app
EOF
